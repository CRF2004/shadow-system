# NOW

Goal: 个人成长游戏化系统 - 通过完成真实任务获取经验值，升级角色、解锁能力
Status: active
Current: Phase 0-9 全部完成。450 测试通过 (2026-09-14 复核)。版本 v0.9.2 (config.VERSION)。日期解析鲁棒性已覆盖全部报告路径（周报/月报/insights/smart-reminders/streak/type-breakdown）+ 公会/副本冷却与过期路径 + Web API 畸形请求体/查询参数/请求头 400 处理（含非对象 JSON body）+ 健康/阅读 JSON/CSV 导入畸形数值与非有限数值跳过 + /api/activities 无限数量（1e999→inf）防断连 + /api/health 非有限睡眠值 400。Phase 9 工作（smart reminders、GitHub 同步、NLU 扩展、state 分离）+ 移动端小屏适配已提交。
  ✅ 2026-09-14: 非有限数值（NaN/Infinity）健壮性 — json.load 默认接受非标准 `Infinity`/`NaN` 字面量，`float("1e999")` 也静默溢出为 inf：health/reading JSON+CSV 导入中 `int(inf)` 抛 OverflowError、NaN sleepHours 在 `_calculate_health_exp`→int() 抛 ValueError（均逃逸出既有 (ValueError, TypeError) 跳过逻辑）→ CLI 导入整条崩溃；Web `/api/health` 的 `_coerce_float` 对 inf/nan 照常返回，`record_health_manual` 内 int(inf) OverflowError 逃逸 → RemoteDisconnected（与 09-04/09-12 同类，已实测）。新增 `_finite_int`/`_finite_float`（非有限即抛 ValueError 交给逐条跳过）+ `_coerce_float` 拒绝非有限返回 None（干净 400）。合法数值行为完全一致。新增 6 个回归测试（先备份源码回退实测 5 个修复前确实崩溃/断连）。444→450 tests 全过 (9.10s)。
  ✅ 2026-09-13: Web API 非对象 JSON 请求体防断连 — 26 个 POST 端点（summon/legion/buy/sell/enter-dungeon/scan-git/scan-files/integrations.enable+disable/auth 注册登录/guilds.* 12 个/onboard.* 2 个）原直接 `body.get(...)`，客户端发送合法 JSON 但非对象（如 `[]`，实测 14 端点 RemoteDisconnected）时 `'list' object has no attribute 'get'` 逃逸 → 连接被直接断开无响应（与 09-04/09-12 同类，此前仅 8 个端点补了 dict 校验）。新增 `_read_object_body()` 统一守卫：非对象返回干净 400，合法对象行为完全一致。新增 2 个回归测试（先 stash 源码实测 AttributeError 断连；遍历 26 端点非对象必 400 + 合法对象 login/guild-info 行为不变）。442→444 tests 全过 (9.88s)。
  ✅ 2026-09-12: /api/activities 无限数量（OverflowError）防断连 — _api_activities 逐条转换 quantity 仍用内联 `int(float(raw))` 且只捕 (TypeError, ValueError)，但 JSON 字面量 `1e999` 会被 json.loads 解析为 inf，`int(float("inf"))` 抛 OverflowError 逃逸 → 连接被直接断开（已实测 RemoteDisconnected，与 09-04 POST 同类）。改用既有 `_coerce_int`（含 OverflowError 防护），畸形条目记入 errors、整个请求返回干净 400；合法数值行为完全一致。新增 2 个回归测试（先 stash 源码验证修复前确实 OverflowError 断连 + 合法 commit 批量仍成功）。440→442 tests 全过 (7.28s)。
  ✅ 2026-09-11: Web API 畸形查询参数/请求头修复 — web_server.py 3 个 summary 端点（health/reading/browser）与 3 个 analytics 端点（weekly/monthly/breakdown）原直接 `int(query_param)` / `int(parse_qs(...)[0])`，遇 `?days=abc`/`?offset=xyz` 抛 ValueError 逃逸，实测连接被直接断开（RemoteDisconnected，与 09-04 POST 同类）。新增 `_query_int` 安全转换，畸形参数返回干净 400 JSON；另将 `_read_body` 的 `int(Content-Length)`（含负数长度）一并防护，不再因恶意请求头崩溃或挂起。合法参数行为完全一致。新增 4 个回归测试（先 stash 源码验证 3 个修复前确实断连），436→440 tests 全过。
  ✅ 2026-09-11: 健康/阅读 JSON 导入畸形数值健壮性 — integrations/health.py import_health_json 与 integrations/reading.py import_reading_json 原对 steps/exerciseMinutes/sleepHours、minutes/pages 直接 int()/float()，外部导出含非数字串（"abc"）或 null 时抛 ValueError/TypeError 逃逸，main.py cmd_health_import/cmd_reading_import 无捕获导致整条 CLI 命令崩溃（CSV 路径早已 try/except 跳过）。现改为捕获 (ValueError, TypeError) 跳过该条、继续处理其余合法条目（合法数据行为完全一致）。新增 2 个回归测试（先复现崩溃验证），434→436 tests 全过 (15.7s)。
  ✅ 2026-09-04: Web API 畸形输入 500/断连修复 — web_server.py 多个 POST 端点原对非数字 quantity/steps/minutes/amount 直接 int()/float()（如 "abc"）抛 ValueError、messages 非 list 时 msg.get AttributeError、顶层 JSON 数组 body.get AttributeError，异常逃逸导致连接被直接断开无 HTTP 响应（已实测 RemoteDisconnected）；新增 _coerce_int/_coerce_float 安全转换 + dict body 校验，8 个端点（record/health/reading/browser/allocate-stat/guild-contribute/guild-deal-damage/chat）畸形输入返回干净 400 JSON，合法数据行为完全一致。新增 9 个回归测试（先 stash 源码验证修复前确实断连），425→434 tests 全过 (7.5s)。
  ✅ 2026-09-03: 公会/副本损坏状态健壮性补齐 — guild.py start_guild_task/start_guild_battle 冷却检查原直接 datetime.fromisoformat(last_task["completedAt"] / last_battle["endedAt"])，遇缺失键 KeyError / malformed 时间 ValueError 直接崩溃；新增 _parse_dt 安全解析（None 视为无冷却，避免坏数据永久锁死成员）。dungeon.py progress_dungeon 遇合法 JSON 但 expires_at 为非 ISO 串时 ValueError 逃逸（外层只捕 JSONDecodeError/OSError）导致整个副本进度扫描崩溃；改为不可解析视为已过期跳过该文件。合法数据行为完全一致。新增 3 个回归测试（先 stash 源码验证修复前确实崩溃），全量 425 tests 全过 (19.6s)。
  ✅ 2026-09-03: analytics 空闲检测损坏数据修正 — get_insights/get_smart_reminders 原用 `max(daily_log, key=原始 date 串)` 找「最近活动」，遇字典序偏高的 malformed 日期（如 "not-a-date"）被误选为最新 → 解析 None 后静默吞掉 idle 提醒/洞察（已复现：4 天无活动却只有兜底文案）。新增 `_latest_activity_date` 按 _parse_date 取最近可解析日期（合法数据行为完全一致）。另 format_weekly_report 按类型统计的「N 天」原按原始 e["date"] 去重，紧凑+ISO 同一天计 2 天（与 type-breakdown 已归一化不一致）；改为按解析后 ISO 去重。新增 3 个回归测试，本会话贡献 419→422；并发会话 guild/dungeon +3 → 全量 425 tests 全过 (19.9s)。
  ✅ 2026-08-31: 日期解析统一收尾（analytics.py 剩余原始字符串用法）— get_type_breakdown 原按 e["date"] 原始字符串去重（days 集合），同一实际日以两种形式存储（如 "20260824" vs "2026-08-24"）会被计为 2 个活跃日，虚高 days_active、拉低 avg_per_day；改为 _parse_date 解析后按 ISO 去重（合法数据行为完全一致）。get_monthly_report 的 best_day/worst_day 原直接返回原始字符串，遇紧凑格式返回非 ISO 串（与周报 best_day 已归一化不一致）；改为 _parse_date 归一化为 ISO。另将 get_insights day_exp 循环的内联 try/except 统一为 _parse_date（行为一致，纯风格统一），get_streak_history best_streak_date 归一化为 ISO（不可解析时回退原值）。新增 2 个回归测试（type-breakdown 紧凑日期只算 1 天 / 月报 best_worst_day 归一化），417→419 tests 全过 (7.65s)。
  ✅ 2026-08-30: 日期解析鲁棒性收尾 — get_weekly_report 原按原始字符串匹配日期（daily_data/missing_days/best_day），遇可解析但非规范形式（如紧凑格式 "20260824"）会错位：days_active=1 但该日 EXP 显示 0、被列入 missing_days、best_day 返回非 ISO 串；改为按 _parse_date 解析后匹配，best_day 归一化为 ISO（合法数据行为完全一致）。另为 get_streak_history 加 createdAt 未来日期防护（时钟偏移/坏导入不再产生负 days_since/completion_rate）。新增 2 测试，实测 417 tests 全过 (40.3s)。
  ✅ 2026-08-30: 报表损坏数据健壮性补齐 — 08-26 的 _parse_date 统一只覆盖了 _filter_week/_filter_days/insights/reminders，但 get_monthly_report 对 None/缺 date 键仍 AttributeError、get_streak_history 对 malformed createdAt 仍 ValueError（均已复现）。新增 _parse_datetime_date 并用 _parse_date 过滤月报条目（合法数据行为完全一致）。新增 2 个回归测试（月报损坏日期不崩溃 / streak 损坏 createdAt 不崩溃），413→417 tests 全过。
  ✅ 2026-08-26: 轻量复审 + 日期解析鲁棒性修复 — analytics.py 统一损坏日期处理：原 _filter_week/_filter_days/get_insights/get_smart_reminders 遇 malformed 日期直接 ValueError 崩溃，而 day_exp 循环已容忍；新增 _parse_date 安全解析并统一使用（合法数据行为完全一致）。新增 3 个测试：smart-reminders 平衡活跃回退文案 + insights/reminders 损坏日期不崩溃（其中 2 个修复前确实 ValueError，已用 stash 验证）。410→413 tests 全过 (实测 22.2s)。
  ✅ 2026-08-22: 版本一致性守卫测试 — 新增 tests/test_version.py (3 tests): config.VERSION==0.9.2 / web_server.VERSION==config.VERSION / main.py docstring 含当前版本号。守卫 08-03 (web_server 硬编码 0.8.0) + 08-08 (main.py docstring v0.4.0) 两个历史 bug 不回归。407→410 tests 全过 (实测 6.56s, load<1)
  ✅ 2026-08-03: 修复 web_server.py 版本显示 bug — 原硬编码 VERSION="0.8.0"，现改为 `VERSION = config.VERSION` (0.9.2)。curl /api/status 验证返回 0.9.2，py_compile ✅，403 测试通过。
  ✅ 2026-08-08: 修复 main.py 模块 docstring 残留 v0.4.0 → 更新为 v0.9.2（运行时显示早已用 VERSION 常量）。全项目版本引用已一致。py_compile ✅，403 测试通过。
  ✅ 2026-08-10: 新增 tests/test_analytics.py 2 个 smart-reminders 测试（test_missing_type_reminder / test_diversity_reminder），覆盖「历史活跃类型回落建议」与「单类型占比过高建议」。403→405 测试全过。
  ✅ 2026-08-11: 移动端就绪回归测试 — test_web.py 新增 test_mobile_viewport_meta_present + test_mobile_responsive_css_media_queries（守卫 viewport meta 与 layout.css 的 768/480 断点），不依赖 Docker 即可防止移动端适配回退。405→407 测试全过；新增 pytest.ini 消除 asyncio 弃用警告。
  ✅ 2026-08-14: 移动端适配验收完成 — plan.md §6.5 任务「手机浏览器也能正常使用」标记完成（viewport meta ✓ + layout.css 768/480 断点 ✓ + 2 个回归测试实测通过）。代码侧已就绪，仅剩真机/Docker 验收项。
  ✅ 2026-08-14: 轻量复审 — 407 tests 全过 (7.9s)、v0.9.2、移动端守卫测试在位、无 TODO/FIXME 残留。项目健康，处于自然完成点。
  ✅ 2026-08-18: 轻量维护复审 — 407 tests 全过 (7.2s)、v0.9.2 一致、无新增 TODO/FIXME。处于自然完成点，无待办推进。
Next: 待 Docker socket 权限恢复后部署 NextChat 并做真机验收；或决定 Phase 10 方向（候选见 Open questions）；其间可继续低风险健壮性加固（外部 JSON 导入/CLI 参数校验等边界）
Blockers:
- Docker socket 权限不足（无法启动 NextChat 容器）— 需联系管理员开通

Open questions:
- 是否需要继续推进 Phase 10 新功能，还是先做体验打磨？候选方向: ① 社交/竞争 (公会排行榜/赛季活动) ② 数据导出与可视化 (web 面板图表增强) ③ PWA 移动端 (可安装+离线) ④ 浏览器插件深化。需用户确认后启动
- Phase 9.5 剩余项：NextChat Docker 部署 + 真机移动端测试（需 Docker/手机权限）

Updated: 2026-09-14
